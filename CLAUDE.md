# CLAUDE.md — dsa-content

A **content repo** (not an app) for the `graphl-ux` engine. It holds the **Data
Structures & Algorithms** concept: notebooks, per-section narration, and the
`manifest.json` that wires them. The app fetches this repo's `manifest.json` +
notebooks over the network and renders them. Read `README.md` first; this file is the
working orientation.

This repo is a sibling of `apache-spark-content`, `java-content`, `scala-content`, and
`python-content` and follows the **same contract** — when in doubt, mirror what
`apache-spark-content` does (its `CLAUDE.md`/`DESIGN.md`/`MODULES.md` are the mature
reference, especially the **TTS guidelines** and the **fixed semantic palette**).

## The core contract (do not break)

1. **The notebook is the single source of truth** for prose and code. `manifest.json`
   only *wires* sections — never duplicate notebook content here.
2. The app splits each notebook at every `## ` heading into sections (= pages) and
   matches them to the manifest by **normalized heading** (lowercase, backticks
   stripped, whitespace collapsed). A heading edit in a notebook must be mirrored in
   the manifest `heading`.
3. Section diagram **images are stripped** by the app — a **scene** replaces them.
4. **Scenes live in `graphl-ux`** (`src/scenes`), authored in TypeScript — not here.
   The `scenes/` dir is reserved/empty. Reference a scene by **id** only.

## The DSA scenes (in graphl-ux, not here)

DSA uses **two** scenes (both live in `graphl-ux/src/scenes`, registered in
`scenes/index.ts`). Conceptual modules ride the taxonomy; structure modules ride the
catalog poster. (The old two-column **Taxonomy ▸ RAM** map was split: the RAM sketches
were removed from `dsa.ts`, and the per-structure diagrams now live in `dsa-catalog`.)

- **`dsa`** — the classical taxonomy (`dsa.ts`): the family tree from
  Primitive/Non-Primitive down to the concrete types. Node ids:
  `dsa-root`, `dsa-primitive`, `dsa-nonprimitive`, `dsa-linear`, `dsa-nonlinear`,
  `dsa-static`, `dsa-dynamic`, `dsa-array`, `dsa-linkedlist`, `dsa-stack`,
  `dsa-queue`, `dsa-tree`, `dsa-graph`. **No `dsa-ram-*` ids exist anymore.**
- **`dsa-catalog`** — the "top data structures" poster (`dsa-catalog.ts`): 12 boxed /
  node-link diagrams. Box (container) ids: `dsa-cat-array`, `dsa-cat-matrix`,
  `dsa-cat-queue`, `dsa-cat-stack`, `dsa-cat-linkedlist`, `dsa-cat-tree`, `dsa-cat-bst`,
  `dsa-cat-heap`, `dsa-cat-trie`, `dsa-cat-graph`, `dsa-cat-unionfind`, `dsa-cat-hashmap`
  (plus fine sub-ids like `dsa-cat-bst-4`, `dsa-cat-ll-n0`).

Highlighting a container id lights its children, so spotlight the box (`dsa-stack`,
`dsa-cat-array`) to light a whole group. A `highlight`/`focus` id must belong to the
**same scene** that section uses — always check `dsa.ts` / `dsa-catalog.ts` for the id
list before adding one.

## manifest.json shape

Top level: `concept`, `design`, `scenes[]` (id/title/status — both `dsa` and
`dsa-catalog`), `presentations[]` (one per module). Each presentation: `id`, `title`,
`notebook`, `defaultScene` (the module's scene — `"dsa"` for 01/02/03/08,
`"dsa-catalog"` for the structure modules), `sections[]`. Each section overlay:
`heading` (must match a notebook `## ` heading, normalized), `scene` (`"dsa"` or
`"dsa-catalog"`), `spine` (bool — feed-mode narration; `false` for
recap/cheat-sheet/summary sections), optional `role` (`"hook"` on the first section),
`highlight` (scene node ids — AMBER, rest dim), `focus` (node id(s) the camera frames;
**omitted** for the taxonomy modules, which show the whole tree), `audio` (repo-relative
`.wav`).

`manifest.json` is generated by `scripts/build_manifest.py`: scene is chosen per module
(`MODULE_SCENE`); within that scene the highlight/focus box is chosen per section by
topic keyword, falling back to a module default. Re-run it after any notebook heading
change.

## Narration

One `.tts` per section, plain spoken prose. Naming `tts/<NN>-<SS>-<slug>.tts` →
`audio/<NN>-<SS>-<slug>.wav`; the stem is shared by the `.tts`, the `.wav`, and the
manifest `audio` field, where `<slug>` is the normalized notebook heading. **Drop the
notebook's intro overview and the trailing "next notebook" outro** — those are
reading-only, not narrated. Follow `apache-spark-content/CLAUDE.md` "TTS guidelines"
verbatim (no markdown/code; spell out symbols/acronyms — `O(n)` → "linear time",
RAM → "ram", `==` → "double equals", `heapq` → "heap-q", `O(h)` → "proportional to the
height").

The source curriculum at `~/Projects/dsa/tts/` has **one `.tts` per notebook** (a
monologue). The per-section files here were authored 1:1 with the notebook `## `
headings, reusing the monologue prose where it aligns and filling sections the
monologue narrated only coarsely (the curriculum monologue is coarser-grained than the
notebook sections, so it could not be split mechanically).

## When wiring a module

1. Confirm the notebook is in `notebooks/` and its `## ` headings are final.
2. Author one `tts/<NN>-<SS>-<slug>.tts` per section (1:1 with the headings).
3. Run `scripts/build_manifest.py` to regenerate `manifest.json` (per-module scene +
   spine/role/highlight/focus/audio). A new module needs an entry in `MODULE_SCENE`
   (and a `*_DEFAULT`) in that script.
4. Run `scripts/colab_generate_audio.ipynb` to generate + push the `.wav`s.

## Status

- **Modules 01–11** scene-wired AND narrated in `manifest.json` (scene/spine/highlight/
  focus/role + per-section `audio`). **101 `tts/*.tts` scripts**, 1:1 with the manifest
  `audio` stems. `.wav`s pending Colab.
- 01/02/03/08 ride the **`dsa` taxonomy** scene (whole tree, topic node lit — RAM
  visuals were removed; richer per-module visuals are a TODO). 04/05/06/07/09/10/11 ride
  the **`dsa-catalog`** poster, camera framing the matching structure box.
- The source curriculum lists 24 modules but only **01–11 exist as notebooks**; the
  rest are unwritten.
- **graphl-ux wiring done:** both scenes (`dsa`, `dsa-catalog`) are registered in
  `graphl-ux/src/scenes/index.ts`, and the DSA concept is in
  `graphl-ux/src/content/catalog.ts` (`contentBaseUrl` → this repo). The app can render
  this content.
- Not yet pushed to GitHub.
